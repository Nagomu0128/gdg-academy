# ジェネリクスの制約

このレッスンで学ぶこと

- 型変数 `T` に「満たすべき形」を条件として付ける
- `extends` を使って「ある性質を持つ型だけ」に限定する
- どんな型でも受け取りつつ、必要なプロパティは安全に使う

<Callout type="info">
入門で学んだジェネリクスは「どんな型でも」でしたが、今回は「id を持つ型だけ」のように絞り込みます。
</Callout>
