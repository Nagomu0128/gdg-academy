# なぜ制約が必要か

制約なしの `<T>` は「どんな型でも」なので、プロパティを使おうとするとエラーになります。

```ts
function pickId<T>(item: T): number {
  return item.id; // T は何か分からないので id を使えない
}
```

- `T` が数値かもしれないし、文字列かもしれない
- だから `item.id` があるとは限らず、安全に使えません

そこで「`id` を持っている型だけ」と条件を付けます。
