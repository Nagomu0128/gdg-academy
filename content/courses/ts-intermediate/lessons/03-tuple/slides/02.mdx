# 配列との違い

ふつうの配列型 `string[]` は「文字列がいくつでも」並べられます。

```ts
const names: string[] = ["ア", "イ", "ウ"];
```

タプル型は角かっこの中に型を並べ、要素数と各位置の型を固定します。

```ts
const pair: [string, number] = ["タロウ", 20];
```

- 1つ目は必ず文字列、2つ目は必ず数値
- 要素は2つちょうど(多くても少なくてもだめ)
- 順番も決まっています(入れ替えられません)
