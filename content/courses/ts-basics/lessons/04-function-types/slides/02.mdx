# 引数と戻り値の型

引数は名前ごとに、戻り値は `)` のうしろに書きます。

```ts
function add(a: number, b: number): number {
  return a + b;
}
```

- 引数: `a: number`、`b: number`
- 戻り値: `): number`(この関数は数値を返す、という意味)
- 型と合わない値を渡すと、実行前にエディタが教えてくれます
