# やってみよう

`first` をジェネリック関数にします。

```ts
function first<T>(items: T[]): T {
  return items[0];
}
```

手順

1. 関数名のうしろに `<T>` を付ける
2. 引数を `items: T[]`、戻り値を `: T` にする
3. `return items[0];` を書き、数値配列と文字列配列の両方で試す
