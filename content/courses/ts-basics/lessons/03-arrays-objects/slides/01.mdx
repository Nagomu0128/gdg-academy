# 配列とオブジェクトの型

このレッスンで学ぶこと

- 配列の型注釈(`number[]` / `string[]`)
- オブジェクトの型注釈(`{ name: string }`)
- 関数の引数・戻り値に配列やオブジェクトの型をつける

<Callout type="info">
配列もオブジェクトも「中身の型」まで書けるのが TypeScript の便利なところです。
</Callout>
