# 配列の型

「要素の型」のうしろに `[]` を付けます。

```ts
let scores: number[] = [80, 95, 70];
let names: string[] = ["みらい", "たろう"];
```

- `number[]` は「数値がならんだ配列」
- `string[]` は「文字列がならんだ配列」
- ちがう型の値を入れようとすると、実行前にエディタが教えてくれます
