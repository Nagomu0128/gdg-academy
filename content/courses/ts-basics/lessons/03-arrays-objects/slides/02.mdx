# 配列の型

「要素の型」のうしろに `[]` を付けます。

```ts
let scores: number[] = [80, 95, 70];
let names: string[] = ["みらい", "たろう"];
```

- `number[]` は「数値がならんだ配列」
- `string[]` は「文字列がならんだ配列」
- 中身の型を書いておくと、その配列に何を入れるつもりかがはっきりします
