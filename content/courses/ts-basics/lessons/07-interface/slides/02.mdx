# interface の書き方

`interface 名前 { ... }` で、オブジェクトが持つプロパティを並べます。

```ts
interface User {
  name: string;
  age: number;
}
```

- `name: string`、`age: number` のように「プロパティ名: 型」を書きます
- 1 行ごとに `;` で区切ります(改行だけでもかまいません)
- これで「User という形」に名前がつきました
